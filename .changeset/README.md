Run `npm run changeset` in any PR that changes behaviour. Pick patch / minor / major
and describe the change for users. The release workflow turns these into a
"Version Packages" PR; merging it publishes to npm with provenance.
